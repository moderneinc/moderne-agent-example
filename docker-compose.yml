services:
  moderne-agent:
    build:
      context: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./example-repos.csv:/app/repos.csv:ro
    restart: unless-stopped
